# Pinned to the versions the bot was last verified against (Python 3.11).
# pymongo ships bson, so bson is not listed separately.
chat_exporter==2.6.1
motor==3.4.0
nextcord==2.6.0
pymongo==4.6.1
python-dateutil==2.8.2
python-dotenv==1.0.1
steam==1.4.4
termcolor==2.4.0
Unidecode==1.3.7
